root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  args_bin = ["-dev"]
  include_ext = ["go"]
  exclude_dir = ["tmp", "dist"]
  delay = 200

[log]
  time = false

[misc]
  clean_on_exit = true
